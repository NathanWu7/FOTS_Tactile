# Python 3.11 兼容依赖（可与 environment.yml 一起使用，或单独 pip install -r requirements-py311.txt）
# 建议在 conda 环境中: conda activate fots_tactile && pip install -r requirements-py311.txt

# 核心版本（已放宽以兼容 Python 3.11，且避免 NumPy 2 的破坏性变更）
numpy>=1.24,<2
opencv-python>=4.5.3,<4.10
torch>=2.0,<2.5
torchvision>=0.15,<0.20
scipy>=1.9,<1.14
pandas>=1.4,<2.2
pillow>=9.2,<11
open3d>=0.15,<0.19
matplotlib>=3.5,<3.9
scikit-learn>=1.1,<1.5
imageio>=2.21,<2.35
tqdm>=4.64,<5
wandb>=0.13,<0.18

# 配置与工具
hydra-core>=1.2,<1.3
# attrdict 已由代码内 AttrDict 实现替代，无需安装

# 硬件/设备（若使用 DIGIT 等传感器再安装）
# digit-interface==0.2.1
# pyudev>=0.23

# 可选：GUI
PyQt5>=5.15

# 其他
certifi>=2022.6
six>=1.16
