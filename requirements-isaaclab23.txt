# 与 Isaac Lab 2.3 依赖一致（Isaac Sim 5.1 + Python 3.11 + PyTorch 2.7）
# 用法：
#   在已有 Isaac Lab 2.3 环境中：pip install -r requirements-isaaclab23.txt
#   或单独建环境：conda env create -f environment-isaaclab23.yml && pip install -r requirements-isaaclab23.txt
# 参考：https://isaac-sim.github.io/IsaacLab/release/2.3.0/source/setup/installation/pip_installation.html

# 与 Isaac Lab 2.3 官方一致
torch==2.7.0
torchvision==0.22.0
# 若需 CUDA 12.8：pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128

# 科学栈（与 PyTorch 2.7 / Python 3.11 兼容，Isaac Lab 环境常见版本）
numpy>=1.26,<3
scipy>=1.11,<1.15
pandas>=2.0,<2.2
pillow>=10,<11
open3d>=0.18,<0.20
matplotlib>=3.7,<3.9
scikit-learn>=1.3,<1.6
imageio>=2.31,<2.35
tqdm>=4.66,<5
wandb>=0.16,<0.18

# FOTS 本工程额外依赖
opencv-python>=4.8,<4.10
hydra-core>=1.2,<1.3
certifi>=2022.6
six>=1.16

# 可选：GUI
# PyQt5>=5.15

# 可选：DIGIT 传感器
# digit-interface==0.2.1
# pyudev>=0.23
